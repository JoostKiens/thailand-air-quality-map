// Viewport bounding box — all data layers and the Mapbox maxBounds align to this extent.
// west, south, east, north — matches backend DEFAULT_BBOX in packages/backend/src/lib/bbox.ts
export const VIEWPORT_BBOX = [89, 1, 114, 30] as const;
